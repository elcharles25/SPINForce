# ============================================
# .gitignore para SPIMForce CRM
# ============================================

# Node modules (dependencias - se instalan con npm install)
spimforce/node_modules/
spimforce-installer/node_modules/


# Variables de entorno (contiene API keys privadas)
spimforce/.env
spimforce-installer/.env

# Datos de usuario y runtime (base de datos, attachments, logs)
spimforce/runtime/
!spimforce/runtime/.gitkeep

# Archivos para generar columnas en BBDD
spimforce/*.py

# Archivos de bloqueo de dependencias (opcional según preferencia)
# Descomenta si quieres que cada uno instale sus propias versiones
spimforce/package-lock.json
spimforce-installer/package-lock.json

# Archivos de inicio y parada de la aplicación (se generan en la instalación)
spimforce/*.bat
spimforce/start-hidden.vbs
spimforce/LEEME.md

# Build outputs (archivos compilados por Vite)
spimforce/dist/
spimforce/dist-ssr/
spimforce/*.local

# Logs generales
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Archivos temporales del backend
spimforce/backend/temp/*
spimforce/backend/temp/inbox_cache/*
!spimforce/backend/temp/.gitkeep
!spimforce/backend/temp/inbox_cache/.gitkeep

# Cache de dependencias
.npm
.eslintcache
.node_repl_history

# Archivos de sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp
*~

# Archivos de TypeScript
*.tsbuildinfo

# Coverage reports
coverage/
*.lcov
.nyc_output

# Archivos de prueba temporales
*.test.local.*
*.spec.local.*